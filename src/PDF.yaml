# PDF.yaml - PDF viewer templates
# ⚠️ Keep comments in this file minimal because it has to get copied at runtime. ⚠️
#
# NOTE: These keys are intentionally empty. The active PDF webview is rendered
# entirely by UIWebView.yaml. The previous (unused) template content is preserved
# in PDF_prev.yaml.

pdf_html: ''
pdf_css: ''
pdf_js: ''
