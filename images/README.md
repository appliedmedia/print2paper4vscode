# images

Extension icon and badge images.

## Files

* `logo-p2p4vsc_128x128px.png` — 128x128 marketplace icon. Referenced by the `"icon"` field in `config/template.package.json` and included in the published VSIX.
* `ci.svg`, `coverage.svg`, `license.svg` — status badges referenced from the root `README.md`.
* `svgs.yaml` — badge source definitions.
* Applied Media logo variants (`logo-appliedmedia-*`) — brand assets, not shipped in the VSIX.
