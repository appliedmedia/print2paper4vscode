# Template replacement configuration for main project
#
# This config is used when running templateDictReplace.mjs from scripts/templateDictReplace/
# For badge-specific config, see scripts/badges4readmes/templateDictReplace.yaml

replacements: []
  # Example entries - customize for your project needs
  
  # Example: Extract from JS module
  # - file: .config/template.package.json
  #   output: package.json
  #   template: "{{extId}}"
  #   source: out/src/types/_entrypoint_extId_t.js
  #   key: kExtId
  
  # Example: Extract from YAML file
  # - file: .config/template.readme.md
  #   output: README.md
  #   template: "{{projectName}}"
  #   source: .config/metadata.yaml
  #   key: projectName
