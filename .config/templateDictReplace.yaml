# templateDictReplace configuration for build process
#
# Matches App.templateDictReplace() - same concept for build files
#
# Each entry specifies:
# - file: Target file to process (relative to project root)
# - output: Output file path (relative to project root)
# - template: Template placeholder to replace (e.g., {{extId}})
# - source: Compiled JS file to import from (relative to project root)
# - variable: Variable name to import (e.g., kExtId)
#
# To add more replacements, just add entries to this array

replacements:
  # Process out/package.json for test builds
  - file: package.json
    output: out/package.json
    template: "{{extId}}"
    source: out/src/_entrypoint_extId_t.js
    variable: kExtId
  # Process root package.json for deployment (vsce reads this)
  - file: package.json
    output: package.json
    template: "{{extId}}"
    source: out/src/_entrypoint_extId_t.js
    variable: kExtId
